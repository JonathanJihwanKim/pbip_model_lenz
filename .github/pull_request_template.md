## Summary

<!-- One paragraph: what this changes and why. -->

## Changes

<!-- Bullet list of the meaningful changes. -->

## Test plan

- [ ] `pytest tests` passes locally
- [ ] If parser-touching: added a fixture in `tests/fixtures/edge_cases/` (when appropriate)
- [ ] If frontend-touching: ran `npm run build` and verified locally with `model-lenz serve`
- [ ] Updated docs (`README.md` / `CONTRIBUTING.md`) if user-visible behavior changed
